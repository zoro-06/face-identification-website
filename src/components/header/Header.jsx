import styles from "./header.module.css"

const Header = () => {
    return (
        <div className={styles.headerDiv}>
            <h1 className={styles.heading}>FACE IDENTIFICATION</h1>
        </div>
    )
}

export default Header;